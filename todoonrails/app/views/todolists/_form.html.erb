<%= form_with(model: @todo, local: true, scope: :todo) do |form| %>

<% if @todo.errors.any? %>
<div id="error_explanation">
    <h2>
        <%= pluralize(@todo.errors.count, "error") %> prohibited
        this todo from being saved:
    </h2>
    <ul>
        <% @todo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
    </ul>
</div>
<% end %>

<p>
    <%= form.label :Description %><br>
    <%= form.text_area :description %>
</p>

<p>
    <%= form.label :Priority %><br>

    <%= form.select :priority, [["High", "High"], ["Moderate", "Moderate"], ["Low", "Low"]] %>
</p>

<p>
    <%= form.label :Priority %><br>

    <%= form.datetime_field :duedate %>
</p>

<p>
    <%= form.submit %>
</p>
<% end %>